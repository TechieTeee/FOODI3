import { HStack } from "@chakra-ui/react";
import React from "react";

export const Socials: React.FC = () => {
  return (
    <HStack color={"white"} spacing={4}>
      {/* Removed the Social Links - Can Create Pages and Add Back Later If Time Permits */}
    </HStack>
  );
};
